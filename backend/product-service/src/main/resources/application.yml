spring:
  application:
    name: product-service
  datasource:
    url: jdbc:postgresql://localhost:5436/ps_db
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        default_schema: public
    show-sql: true
server:
  port: 8082

springdoc:
  swagger-ui:
    path: /swagger-ui.html

eureka:
  instance:
    prefer-ip-address: true


whitelisted-paths: /swagger-ui/**, /v3/api-docs/**, /v3/api-docs.yaml, /swagger-ui.html, /bus/**, /actuator/**
orders-cron-active: true
stripe-key: sk_test_51RRHph67CPFfX2OAI3N6tqY2WlHkd3VH4s7m9MBCty88dYbNp5pbFFGFtfupeQSiZKQwAqiripuU8U5ZkX87cbW8000otYzES3